/**
 * useAuth Hook
 * Convenience hook to access authentication functionality
 * Re-exports the useAuthContext for easier imports
 */

export { useAuthContext as useAuth } from '../context/AuthContext';
